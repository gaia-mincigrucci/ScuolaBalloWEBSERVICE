CREATE DATABASE db_scuolaballo;
USE db_scuolaballo;

CREATE TABLE corsi (
    id INT AUTO_INCREMENT PRIMARY KEY,
    stile VARCHAR(100) NOT NULL,
    orario VARCHAR(100) NOT NULL,
    posti_disponibili INT NOT NULL
);

CREATE TABLE prenotazioni (
    id INT AUTO_INCREMENT PRIMARY KEY,
    corso_id INT NOT NULL,
    email_allievo VARCHAR(150) NOT NULL,
    data_prenotazione TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (corso_id) REFERENCES corsi(id)
);

-- dati di prova
INSERT INTO corsi (stile, orario, posti_disponibili) VALUES 
('Salsa Cubana', 'Lunedì 20:00', 10),
('Hip Hop Base', 'Martedì 17:30', 2),
('Danza Classica', 'Giovedì 16:00', 0);
